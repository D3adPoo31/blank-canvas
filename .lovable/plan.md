Substituir o placeholder em `src/routes/index.tsx` por uma tela completamente em branco (apenas uma `div` ocupando a tela inteira com fundo branco, sem conteúdo).

Nenhuma outra alteração: sem rotas adicionais, sem componentes, sem estilos extras.